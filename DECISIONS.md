# DECISIONS.md — قرارات غير مغطاة في SPEC.md

> SPEC.md هو القانون. هذا الملف يوثّق فقط القرارات التي لم يغطها، مع السبب.

## D1 — styleguide.html كتبويبات ملء الشاشة (2026-06-11)
SPEC §7 يطلب عرض كل عناصر الهوية في styleguide.html، و§7/agent-rules تفرض "صفر تمرير عمودي عند 360×640".
كل العناصر لا تتسع في شاشة واحدة، فجُعل الدليل تطبيقًا مصغّرًا بأربعة تبويبات
(الألوان / الخطوط / العناصر / ليل-نهار) — كل تبويب يلتزم بقاعدة عدم التمرير،
ويُحاكي بنية شاشات اللعبة نفسها. التبويبات قابلة للربط المباشر عبر الـ hash
(`#p-colors` `#p-fonts` `#p-comp` `#p-phase` و`#p-phase:day` لحالة النهار) لتسهيل المراجعة.

## D2 — شريط السدو CSS خالص بلا صور (2026-06-11)
نُفِّذ شريط السدو بـ `conic-gradient` (مثلثات متكررة بأحمر السدو وبرتقالي الفانوس على أرضية داكنة)
بدل صورة، التزامًا بمبدأ "اللعبة تعمل كاملة حتى لو غاب أي أصل" (SPEC §8) — الشريط لا يعتمد على ملف.

## D4 — عقدتا `phaseInfo` و`report` إضافة على نموذج البيانات (2026-06-11)
SPEC §5 لا يتضمنهما، و§6 يجيز تعديل المسارات مع حفظ الضمانات. `phaseInfo` (كتابة هوست/قراءة الجميع)
يحمل `endsAt` للمؤقت المشترك بين الأجهزة و`voteKey/candidates` لإعادة التصويت؛ `report` يحمل إعلان
الصباح/الإعدام بشكل مهيكل بدل تفكيك نصوص `publicLog`. مفتاح إعادة التصويت `«N»r` يمر عبر
`votes/$round` نفسها.

## D5 — الهوست يقرأ `secrets` كلها (2026-06-11)
حسم الليل يتطلب معرفة الأدوار، ولا يوجد خادم (SPEC §1: لا Cloud Functions). القاعدة تمنح القراءة
لمالك السر **وللهوست فقط**. مقصود وموثق: الهوست لاعب موثوق بحكم التصميم — من يفتح الكونسول
من جهاز الهوست يرى الأدوار. هذا سقف النموذج بلا خادم.

## D6 — «الأغلبية» في التصويت = الأغلبية النسبية (2026-06-11)
SPEC §4.6 يقول "Majority → execution. Tie → revote". فُسّرت أغلبيةً نسبية (الأعلى أصواتًا يُعدم،
التعادل على القمة → إعادة بين المتعادلين فقط، تعادل ثانٍ → لا إعدام) — وهي قاعدة اللعبة الكلاسيكية.
صفر أصوات = لا إعدام. التصويت على النفس ممنوع واجهةً.

## D7 — نتيجة العرّاف تُسلَّم أثناء الليل نفسه (2026-06-11)
المحرك يجيب فحص العرّاف فور وصوله (`seerLiveAnswer`) بدل انتظار نهاية الليل — التجربة
الكلاسيكية للدور، وSPEC لا يحدد التوقيت. تُكرَّر النتيجة بإشعار خاص في تقرير الصباح.

## D8 — كتابة الهوست على chat/votes/nightActions حذفٌ فقط (2026-06-11)
اكتُشف حيًا على المحاكي: قاعدة تنظيف الهوست (`write: host`) سمحت لهوستٍ **ميت** بإرسال شات.
القيد `&& !newData.exists()` يبقي تنظيف «العب مرة ثانية» ويمنع الإدراج. الأسرار وplayers تبقى
كتابة هوست كاملة (يحتاجها فعليًا: الأدوار، seerResults، alive، misses).

## D9 — استثناءان من قاعدة «صفر تمرير» (2026-06-11)
سجل الشات وقائمة كشف الأدوار في النتيجة لهما تمرير داخلي (`overflow-y` داخل صندوق ثابت) —
المحتوى فيهما غير محدود بطبيعته، والشاشة نفسها تبقى `100dvh` بلا تمرير صفحة. شبكات اللاعبين
تتكثّف لأربعة أعمدة فوق ٩ عناصر لتبقى داخل الشاشة.

## D10 — عدّاد AFK (أونلاين فقط) (2026-06-11)
`misses` يحتسب على أصحاب الأدوار الذين فوّتوا فعل الليل وعلى كل حي فوّت التصويت؛ أي فعل/صوت
يصفّره. عند ≥2 تظهر 💤 في شبكة التصويت. لا احتساب في وضع المجلس (الكل حاضر بالمكان).

## D11 — أدوات تطوير: `?emu` و`?fast` ومحاكي Firebase (2026-06-11)
`?emu` يوجّه التطبيق لمحاكيات Auth/RTDB المحلية (لاختبار مُنفَّذ القواعد بلا إنتاج)،
و`?fast` يقسم مدد المراحل على ١٠ لاختبار حلقات كاملة بدقائق. لا أثر لهما على الإنتاج.

## D3 — خادم تطوير محلي بسيط (2026-06-11)
أُضيف `tools/dev-server.mjs` (خادم ملفات ثابتة بـ Node بلا أي تبعيات) + `.claude/launch.json`
للمعاينة المحلية فقط. الإنتاج يبقى ملفات ثابتة على GitHub Pages بلا أي خطوة بناء (SPEC §2).
ملاحظة: بايثون النظام (3.9) فشل داخل صندوق الأذونات، لذا اختير Node.
